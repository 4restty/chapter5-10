<h2>New Book</h2>
<%= form_with model: @book, local:true do |f| %>
<div>
  <%= f.label "Title" %><br>
  <%= f.text_field :title %>
</div>
<div>
  <%= f.label "Opinion" %><br>
  <%= f.text_area :body %>
</div>
<%= f.submit "Create Book" %>
<% end %>
